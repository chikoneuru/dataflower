# Docker Compose for Ours Scheduler
# Uses warm function containers from docker-compose-warm-pools.yml

networks:
  dataflower-shared:
    external: true
    name: docker_dataflower-shared

services:
  # Ours scheduler uses existing function containers from warm-pools
  # No additional services needed - just reference the shared network